const express = require('express');
const router = express.Router();
// All order-related controller functions have been removed as requested.
const { authenticateToken } = require('../auth/middleware');



module.exports = router;
